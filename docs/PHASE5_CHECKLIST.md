# Фаза 5 — Полировка и демо

Этот чек-лист покрывает задачи из `PROJECT_PLAN.md` (фаза 5) и привязан к подготовленным демо-материалам.

## Документация
- [ ] Обновить Swagger/OpenAPI для всех сервисов (gateway + микросервисы). Экспортировать в `docs/api/` и подключить к UI.
- [ ] Руководство пользователя: сценарии Generate/Tasks/Optimize, примеры запросов, FAQ.
- [ ] Примеры использования: ссылки на `demo_materials/manual` и `demo_materials/api`, инструкция по загрузке и скачиванию ZIP артефактов.
- [ ] Видео-демо: записать walkthrough (генерация UI кейсов + GitLab pipeline + аналитика).

## Тестирование
- [ ] Unit: довести покрытие до 60%+ в `backend/*` и `frontend/dashboard` (pytest + vitest/cypress компоненты).
- [ ] Интеграционные: e2e flow "spec → generate → download" против docker-compose (см. `QUICK_TEST.md`).
- [ ] E2E критические сценарии (UI): авторизация, загрузка файла, запуск генерации, просмотр задач, скачивание артефакта.

## Производительность
- [ ] Оптимизация LLM запросов: batch для однотипных шагов, агрегация подсказок, таймауты.
- [ ] Кэширование: результаты парсинга OpenAPI + LLM ответов (Redis TTL).
- [ ] Мониторинг/алерты: экспорт метрик в Prometheus; базовые правила по latency, error rate, queue depth.

## Демо-материалы
- [x] Примеры спецификаций: `demo_materials/manual/ui-calculator-openapi.yaml`, `demo_materials/api/evolution-compute-sample.yaml`.
- [ ] Сценарий демо: шаги для UI + GitLab (см. `demo_materials/README.md`).
- [ ] Скриншоты/документация: собрать после прогона пайплайна и UI.
- [ ] Метрики: замерить время генерации 5–10 кейсов (<30 c) и % корректных тестов (>80%), сохранить в `docs/metrics/`.

## GitLab интеграция
- [x] Тестовый проект: `gitlab-test` (lint + test + coverage).
- [ ] Подключить проект в UI, запустить анализ покрытия/дубликатов, сохранить отчёт в `demo_materials/gitlab/`.

